pipeline {
  agent {label 'jenkins-agent'}
  tools {
    jdk 'jdk'
    maven 'maven'
  }
  stages {
    stage("cleanup-workspace") {
      steps {
        cleanWs()
      }
    }
    stage('checkout-from-scm') {
      steps {
        git branch: 'main', credentialsId: 'github', url: 'https://github.com/akashsakore/register-app.git' 
      }
    }
    stage('buils-application') {
      steps {
        sh 'mvn clean package'
      }
    }
    stage('mvn-test') {
      steps {
          sh 'mvn test'
      }
    }
  }
}
